import { tv } from 'tailwind-variants'

export const skeletonVariants = tv({
    slots: {
        root: 'animate-pulse rounded-md bg-neutral-200 dark:bg-neutral-800'
    },
    variants: {},
    defaultVariants: {}
})
